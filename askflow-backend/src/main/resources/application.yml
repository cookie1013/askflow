server:
  port: 8081

spring:
  application:
    name: askflow-backend

  datasource:
    url: jdbc:mysql://localhost:3306/askflow?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: askflow_user
    password: Askflow@123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

askflow:
  ai-service:
    base-url: http://127.0.0.1:8001